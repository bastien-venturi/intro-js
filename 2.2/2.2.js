let a = prompt('min');
let b = prompt('max');
let c = prompt('current');

if (b <= a){
    console.log('get out of my page!');
    }
    else if(a <= c <= b){
    console.log(c +' is between '+a+' and '+b);
}